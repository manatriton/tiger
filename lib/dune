(library
 (name tiger)
 (libraries core)
 (preprocess
  (pps ppx_jane)))

(env
 (dev
  (flags (:standard -warn-error -30))))

(menhir
 (modules parser)
 (flags --explain --external-tokens Ast --trace))

(ocamllex lexer)